<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    <h1>{{title}}</h1>
    <p>{{{post.contentReplace}}}</p>
    {{#if post.imageUrl}}
        <img src="{{post.imageUrl}}" alt="Post Image">
    {{/if}}
    <p>Posted on: {{post.date}}</p>
</body>
</html>
